<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="$(ProjectDir)..\Common.items" />
  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <GenerateBindingRedirectsOutputType>true</GenerateBindingRedirectsOutputType>
    <ApplicationIcon />
    <OutputType>Exe</OutputType>
    <StartupObject />
    <RootNamespace>Reko.CmdLine</RootNamespace>
    <AssemblyName>Reko.CmdLine</AssemblyName>
    <Platforms>x86;x64;ARM64</Platforms>
    <Configurations>Debug;Release</Configurations>
  </PropertyGroup>

  <ItemGroup>
    <None Include="App.config" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="K4os.Compression.LZ4" Version="1.2.16" />
  </ItemGroup>

  <Import Project="$(ProjectDir)..\PreBuild.targets" />

  <Target Name="CopyToDecompileExe" AfterTargets="CoreBuild">
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.exe" DestinationFiles="$(TargetDir)reko.exe" />
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.dll" DestinationFiles="$(TargetDir)reko.dll" />
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.exe" DestinationFiles="$(TargetDir)decompile.exe" />
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.dll" DestinationFiles="$(TargetDir)decompile.dll" />
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.runtimeconfig.json" DestinationFiles="$(TargetDir)reko.runtimeconfig.json" />
    <Copy SourceFiles="$(TargetDir)Reko.CmdLine.runtimeconfig.json" DestinationFiles="$(TargetDir)decompile.runtimeconfig.json" />
  </Target>
</Project>